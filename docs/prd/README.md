# PRDs

Use [TEMPLATE.md](TEMPLATE.md) when adding a new PRD. New PRDs should keep the
YAML front matter in sync with the body and should preserve the shared section
order unless the PRD genuinely needs a different shape.
